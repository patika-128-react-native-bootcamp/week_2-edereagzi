import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    text_input: {
        borderWidth: 1,
        borderRadius: 5,
        backgroundColor: '#fafafa',
        height: 30,
        padding:5
      },
      text_input_header: {
        color: '#616161',
      }
})